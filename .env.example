# Copy this file to .env and adjust for your local setup.
# Keycloak redirect URI — must match the port the app is exposed on.
# Default (Docker): http://localhost:3000/signin-oidc
# Native run example: http://localhost:5000/signin-oidc
Keycloak__RedirectUri=http://localhost:3000/signin-oidc
